# Handoff

Questo file non è più aggiornato a ogni sessione.

Lo storico discorsivo vive in **`sessioni/`**, un file per sessione, nome = timestamp UTC. A inizio sessione: leggere `status-cpp.md` e **il file più recente** in `sessioni/`.
